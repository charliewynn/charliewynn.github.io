Start the Jekyll dev server using Homebrew's Ruby bundle:

```
/opt/homebrew/opt/ruby/bin/bundle exec jekyll serve
```

Run it in the background and let the user know the server is available at http://localhost:4000. Remind them that _config.yml changes require a server restart.
